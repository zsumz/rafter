\* Temporary PR bounds while the invariants refactor is in progress.
\* Restore RaftCi.cfg for the final canonical branch proof.
SPECIFICATION Spec

CONSTANTS
  Nodes = {n1, n2}
  Values = {v1}
  MaxTerm = 2
  MaxLogLen = 1
  ReadRequests = {r1}

INVARIANTS
  TypeOK
  ElectionSafety
  LogMatching
  LeaderCompleteness
  CommittedPrefixStability
  StateMachineSafety
  StaleLeaderFencing
  CommittedEntriesHaveQuorum
  ReadBarrierLinearizability

CHECK_DEADLOCK FALSE

SYMMETRY ModelPermutations
