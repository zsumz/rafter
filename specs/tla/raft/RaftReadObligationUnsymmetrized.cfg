\* Read-fencing proof obligation, unsymmetrized. Weekly symmetry audit.
\*
\* Identical to RaftReadObligation.cfg with `SYMMETRY ModelPermutations`
\* deliberately absent; see RaftCoreObligationUnsymmetrized.cfg for why the
\* weekly tier carries unsymmetrized twins of its gating obligations at all.
\*
\* The permutation group here has order four (2 nodes x 2 values x 1 read
\* request), and the measured distinct count is 395,573 against a full-orbit
\* prediction of 4 x 98,948 = 395,792 -- a deficit of 219 states, ratio
\* 3.9978, the symmetric states' missing orbit mass (see
\* RaftCoreObligationUnsymmetrized.cfg). A future change in either number of
\* the pair is symmetry-soundness evidence.
\*
\* Calibration (TLC 2026.08.11.125311, -workers 4 -seed 2026081101 -fp 0,
\* -fpmem 0.45, no symmetry): exhausts in 60 seconds -- 2,368,355 states
\* generated, 395,573 distinct, 0 left on queue. Floors are those exact
\* counts.
\*
\* Run it directly:
\*   java -XX:+UseParallelGC -Xmx8g -cp tools/cache/tla2tools.jar tlc2.TLC \
\*     -workers 4 -config RaftReadObligationUnsymmetrized.cfg Raft.tla

SPECIFICATION ReadSpec

CONSTANTS
  Nodes = {n1, n2}
  Values = {v1, v2}
  MaxTerm = 2
  MaxLogLen = 2
  ReadRequests = {r1}

INVARIANTS
  TypeOK
  ElectionSafety
  LogMatching
  LeaderCompleteness
  CommittedPrefixStability
  StateMachineSafety
  StaleLeaderFencing
  CommittedEntriesHaveQuorum
  ReadBarrierLinearizability

CHECK_DEADLOCK FALSE
