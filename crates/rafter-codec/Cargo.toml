[package]
name = "rafter-codec"
version = "0.1.0"
description = "Current peer-message wire codec for Rafter."
readme = "README.md"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
rafter = { path = "../rafter" }

[dev-dependencies]
criterion = { version = "0.5", default-features = false }
proptest = "1"

[[bench]]
name = "codec"
harness = false

[lints]
workspace = true
