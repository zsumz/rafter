[package]
name = "rafter-invariant-test"
version = "0.0.1"
description = "Typed test-oracle markers for Rafter's deterministic invariant gate."
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
rafter-invariant-test-macros = { path = "../rafter-invariant-test-macros" }

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31.3", default-features = false, features = ["socket"] }

[dev-dependencies]
trybuild = "1"

[lints]
workspace = true
