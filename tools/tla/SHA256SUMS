# tla2tools.jar from https://github.com/tlaplus/tlaplus/releases/download/v1.8.0/
#
# Upstream v1.8.0 is a ROLLING NIGHTLY CHANNEL, not an immutable release. The
# tag stays fixed while its assets are rebuilt, re-uploaded under fresh asset
# IDs, and the predecessors deleted. Three distinct jar digests were observed
# in five weeks, and the previously pinned asset 481553986 now 404s outright,
# so an asset ID is a liveness pin, not an identity pin. This is asset
# 510140106, uploaded 2026-08-11T12:59:18Z, reporting TLC 2026.08.11.125311.
#
# The sha256 below is therefore the only stable defense: scripts/tla-model-check
# refuses any jar that does not match it, and the profile contract in
# verification/raft-invariant-profiles.json pins the same digest independently,
# so a silently swapped upstream asset fails closed instead of being accepted.
# A repo-controlled mirror of the exact reviewed jar is the durable fix for the
# liveness half of the problem; adopting one is a maintainer decision and is
# deliberately not made here.
ab323b79802aedc3203b3f9af37c6aca3ed43f4e0225b36f2aa77b26de46c05f  tla2tools.jar
